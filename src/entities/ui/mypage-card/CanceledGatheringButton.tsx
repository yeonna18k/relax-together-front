'use client';
import { Button } from '@/shared/ui/button';

export default function CanceledGatheringButton() {
  // TODO: 예약 취소하기 버튼 클릭 시 예약 취소 API 호출
  return <Button variant="outline">예약 취소하기</Button>;
}
